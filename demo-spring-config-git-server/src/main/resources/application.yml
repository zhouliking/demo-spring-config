server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: http://xxx/zl-spring-config.git
          password: xxxx
          search-paths: config
          default-label: master
          clone-on-start: true

